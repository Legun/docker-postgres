machine:
  services:
    - docker

dependencies:
  override:
    - docker info

test:
  override:
    - docker build -t threew82/postgresql .
    - docker run -d --name=postgresql threew82/postgresql; sleep 10
    - docker run -it --volumes-from=postgresql threew82/postgresql sudo -u postgres -H psql -c "\conninfo"
